import BottomNavigation from "./BottomNavigation"

const Layout = ({ children }) => {
    return (
        <>
            {children}
            <BottomNavigation />
        </>
    )
}

export default Layout
